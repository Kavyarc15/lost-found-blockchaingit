// The contract address and chain ID for the local Hardhat network.
// After deploying, the deploy script auto-generates LostAndFound.json
// which overrides these defaults.

export const HARDHAT_CHAIN_ID = 31337
export const LOCAL_RPC_URL = 'http://127.0.0.1:8545'

// Polling interval for event/notification updates (ms)
export const POLL_INTERVAL = 10000
